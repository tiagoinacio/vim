CS_LAST_WORKSPACE: functional-tests
CS_WORKSPACE_BEGIN: functional-tests
let SessionLoad = 1
if &cp | set nocp | endif
let s:so_save = &so | let s:siso_save = &siso | set so=0 siso=0
let v:this_session=expand("<sfile>:p")
silent only
exe "cd " . escape(expand("<sfile>:p:h"), ' ')
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
set shortmess=aoO
badd +0 protractor.conf.js
badd +0 protractor.overrides.js
argglobal
silent! argdel *
edit protractor.conf.js
let t:ctrlspace_label = 'protractor'
edit protractor.conf.js
set splitbelow splitright
set nosplitbelow
wincmd t
set winheight=1 winwidth=1
argglobal
let s:l = 1 - ((0 * winheight(0) + 20) / 40)
if s:l < 1 | let s:l = 1 | endif
exe s:l
normal! zt
1
normal! 0
tabnew
let t:ctrlspace_label = 'mock'
set splitbelow splitright
set nosplitbelow
wincmd t
set winheight=1 winwidth=1
tabnew
let t:ctrlspace_label = 'smoke'
set splitbelow splitright
set nosplitbelow
wincmd t
set winheight=1 winwidth=1
tabnew
let t:ctrlspace_label = 'end2end'
set splitbelow splitright
set nosplitbelow
wincmd t
set winheight=1 winwidth=1
tabedit protractor.overrides.js
edit protractor.overrides.js
let t:ctrlspace_label = 'override'
edit protractor.overrides.js
set splitbelow splitright
set nosplitbelow
wincmd t
set winheight=1 winwidth=1
argglobal
let s:l = 1 - ((0 * winheight(0) + 20) / 40)
if s:l < 1 | let s:l = 1 | endif
exe s:l
normal! zt
1
normal! 0
tabnext 1
if exists('s:wipebuf')
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=999 winwidth=84 shortmess=filnxtToOc
let s:sx = expand("<sfile>:p:r")."x.vim"
if file_readable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &so = s:so_save | let &siso = s:siso_save
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
CS_WORKSPACE_END: functional-tests
